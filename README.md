## Build Instructions

After changing the rust code you will need to recompile it with the
following command

```bash
~/.cargo/bin/wasm-pack build --target web
```

## Uploading

Just git push to main
